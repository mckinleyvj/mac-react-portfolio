// import axios from 'axios';

// const { REACT_APP_LOCATIONAPI } = process.env;

// const locationapi = (postcode) => axios.get(`${REACT_APP_LOCATIONAPI}${postcode}.json`);

// export default locationapi;
